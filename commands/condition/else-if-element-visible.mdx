---
title: "Else If 元素可见"
description: "前置条件不成立时，检测指定网页元素是否可见。"
---

## 指令说明

**描述：** 检测元素在网页中是否可见，通常与指令【If 元素可见】结合使用。当 If 元素可见执行的结果为 false 的时候，再执行 Else If 元素可见指令进行判断。

## 参数说明

<Tabs>
  <Tab title="常规">
    | 参数 | 说明 |
    | --- | --- |
    | **网页对象** | 选择一个之前通过【打开网页】或【获取已打开的网页对象】指令创建的网页对象 |
    | **检测网页是否** | 可见 / 不可见 |
    | **操作目标** | 从元素库中选择一个已捕获的元素或通过「捕获新元素」来捕获新的网页元素作为操作目标 |
  </Tab>
  <Tab title="高级">
    本指令暂无高级参数。
  </Tab>
</Tabs>

## 使用示例

该指令的使用方法和【If 元素可见（web）】一致，需要结合 If 元素可见（web）指令进行使用。

![Else If 元素可见 - 流程搭建](images/else-if-elem-01.png)

![Else If 元素可见 - 运行结果](images/else-if-elem-02.png)

<Info>
  使用指令过程中遇到问题？前往 [八爪鱼RPA 开发者社区问答板块](https://rpa.bazhuayu.com/community/questions) 提问，获取官方与社区帮助。
</Info>
